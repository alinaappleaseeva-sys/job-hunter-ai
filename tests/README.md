# Tests

`tests/` is for deterministic behavior checks.

Use unit tests for pure logic and integration tests for module boundaries.
Use `evals/` when behavior quality depends on labeled data rather than binary correctness.

