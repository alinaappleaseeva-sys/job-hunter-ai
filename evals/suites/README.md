# Evaluation Suites

This directory will hold runnable evaluation definitions.

Recommended first suite files:

- `ingestion_smoke.yaml` ✅ (Phase 8)
- `normalization_v1.yaml` ✅ (Phase 4)
- `dedup_regression.yaml` ✅ (Phase 8)
- `ranking_topk.yaml` ✅ (Phase 6)
- `ghosting_precision.yaml` ✅ (Phase 7)
